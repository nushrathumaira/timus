#include<stdio.h>
#include<math.h>
#include<iostream>.h>
#include<fstream>
#include<string.h>
#include<sstream>
#include<algorithm>

using namespace std;
/*

int main() {
	//double x1, x2, y1, y2;
	ofstream myfile;
	stringstream ss;

	//scanf_s("%lf %lf %lf %lf",&x1, &x2, &y1, &y2);
	//double d1 = sqrt((x2 - x1));
	//double d2 = sqrt((y2 - y1));
	//printf("%lf %lf", d1, d2);

	

	myfile.open("C:/Users/nushrat humaira/Documents/Visual Studio 2015/Projects/BookEx/output.txt", ios::app);
	if (myfile.is_open())
	{
		
		for (int n = 0; n < 100; n++) {
			
			if(n>0){
				string nstring_ = std::to_string(n);
				string revstring_ = nstring_;
				std::reverse(revstring_.begin(), revstring_.end());


				if (revstring_ == nstring_)
				{
					myfile << nstring_ << endl;
				}
			}
			
						
		}
	}
	myfile.close();
	return 0;
}

*/